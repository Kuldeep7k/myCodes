Module Module1

    Sub Main()
        Dim a, b As Integer
        Dim add, subs, div, multi As Integer
        Console.WriteLine("Enter a")
        a = Console.ReadLine()
        Console.WriteLine("Enter a")
        b = Console.ReadLine()
        add = a + b
        Console.WriteLine("Addition {0}", add)
        subs = (a - b)
        Console.WriteLine("Substract {0}", subs)
        multi = (a * b)
        Console.WriteLine("mutli {0}", multi)
        div = (a / b)
        Console.WriteLine("div {0}", div)


    End Sub

End Module
